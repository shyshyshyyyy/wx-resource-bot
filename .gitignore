# ============================================================
# 本仓库只发布「exe + 说明文档」，源码与开发环境不在此仓库中。
# 采用白名单模式：默认忽略一切，仅放行下方白名单文件。
# ============================================================

# 忽略所有文件与目录
*

# ---- 白名单（必须保留的发布文件）----
!.gitignore
!README.md
!微信资源搜索机器人.exe

# .git 目录由 git 自身管理，列在此处仅为显式声明不被忽略
!.git
